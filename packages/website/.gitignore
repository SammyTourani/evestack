out/
/.generated/

# Output of the QA capture scripts. These were committed once — 94 PNGs, 20 MB,
# imported by nothing — so every clone paid to download screenshots proving a
# gradient looked right on one afternoon. They are regenerable by definition;
# that is what makes them output rather than source.
/qa-shots/
/assets/screenshots-raw/
.firecrawl/
.source/
